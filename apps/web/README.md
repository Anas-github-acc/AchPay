# web

Scaffold only. This is where the dashboard lands: the ledger feed, the chain
verify button, the adversarial grid read from `data/adversarial-results.json`,
and the product feed.

It reads the API over HTTP and shares its types through `@storefront/shared`.
Nothing here is wired up yet.
